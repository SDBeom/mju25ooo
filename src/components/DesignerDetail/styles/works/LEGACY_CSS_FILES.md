# 레거시 CSS 파일 목록 (디바이스 접미사 없음)

다음 파일들은 디바이스 접미사(Desktop/Tablet/Mobile)가 없는 레거시 파일들입니다.
대부분의 작품은 이미 디바이스별 CSS 파일이 있으므로, 이 파일들은 사용되지 않을 가능성이 높습니다.

## 사용되지 않는 것으로 보이는 파일들

### 숫자로 된 파일
- `1.css`
- `1-tablet.css`
- `2.css`
- `2-mobile.css`

### 작품별 레거시 파일 (디바이스별 버전이 이미 존재)
- `AnimationReel.css` → `ParkHeechan2025AnimationReelDesktop.css` 등으로 대체됨
- `AnseonminDots.css` → `AhnSeonminDotDotDotDesktop.css` 등으로 대체됨
- `Caravan.css` → `KimJinaCaravanDesktop.css` 등으로 대체됨
- `CardOfLove.css` → `SongDaheeACardOfLoveDesktop.css` 등으로 대체됨
- `Cellestial.css` → `KimJinaCellestialDesktop.css` 등으로 대체됨
- `ChromeSeasons.css` → `ParkHaeinChrome4SeasonsDesktop.css` 등으로 대체됨
- `CyberJesasang.css` → `KimChaeYoungCyberJesasangDesktop.css` 등으로 대체됨
- `DotianhongAnimated.css` → `DoTienHongSeeTinhAnimatedMvDesktop.css` 등으로 대체됨
- `DotianhongIsometric.css` → `DoTienHongSeeTinhIsometicDesktop.css` 등으로 대체됨
- `FateBoardgame.css`
- `Gofetch.css` → `KimJaeeunGoFetchDesktop.css` 등으로 대체됨
- `GoodbyeUniverse.css`
- `HelloUniverse.css` → `KimYunjungHelloUniverseDesktop.css` 등으로 대체됨
- `HeoJihoonEternalVision.css` → `HeoJihoonEternalVisionDesktop.css` 등으로 대체됨
- `HeoJihoonMasterImagination.css` → `HeoJihoonMasterImaginationDesktop.css` 등으로 대체됨
- `Hifive.css` → `SeoDongbeomHiFiveDesktop.css` 등으로 대체됨
- `JeongitaeDolorSaga.css` → `JeongitaeDolorSagaDesktop.css` 등으로 대체됨
- `JeongitaeWarOrb.css` → `JeongitaeWarOrbDesktop.css` 등으로 대체됨
- `JeonseorinKaron.css` → `SeoRinKaronDesktop.css` 등으로 대체됨
- `JeonseorinRavenX.css` → `SeoRinRavenXDesktop.css` 등으로 대체됨
- `JohaneulTheReason.css` → `ChoHaneulTheReasonThatILiveDesktop.css` 등으로 대체됨
- `JohaneulYouthDream.css` → `ChoHaneulYouthDreamDesktop.css` 등으로 대체됨
- `JungjiminTheGothic.css` → `JungJiminTheGothicDesktop.css` 등으로 대체됨
- `JungjiminTheWeapon.css` → `JungJiminTheWeaponDesktop.css` 등으로 대체됨
- `LeedayoungPlottingRoom.css` → `LeeDayoungPlottingRoomDesktop.css` 등으로 대체됨
- `LeedayoungReadyToMerry.css` → `LeeDayoungReadyToMerryDesktop.css` 등으로 대체됨
- `LeegabiAbyssRacing.css` → `LeeGabiAbyssRacingDesktop.css` 등으로 대체됨
- `LeegabiOvercooked.css` → `LeeGabiOvercookedDesktop.css` 등으로 대체됨
- `LeejiminLelabo.css` → `LeeJiminLeLaboDesktop.css` 등으로 대체됨
- `LeejiminVeneti.css` → `LeeJiminVenetiDesktop.css` 등으로 대체됨
- `LeeunLayered.css` → `LeeUnLayeredDesktop.css` 등으로 대체됨
- `Linked.css` → `ShimSungbinLinkedDesktop.css` 등으로 대체됨
- `love-at-rust-sight.css` → `KimYunjungLoveAtRustSightDesktop.css` 등으로 대체됨
- `love-at-rust-sight-desktop.css` → `KimYunjungLoveAtRustSightDesktop.css` 등으로 대체됨
- `LoveAtRustSight.css` → `KimYunjungLoveAtRustSightDesktop.css` 등으로 대체됨
- `ParkheechanAnimationReel.css` → `ParkHeechan2025AnimationReelDesktop.css` 등으로 대체됨
- `ParkheechanPledge.css` → `ParkHeechanPledgeDesktop.css` 등으로 대체됨
- `ParkjinaCrossCooty.css` → `ParkJinaCrossCootyDesktop.css` 등으로 대체됨
- `ParkjinaRemains.css` → `ParkJinaRemainDesktop.css` 등으로 대체됨
- `Peony.css` → `SongDaheePeonyDesktop.css` 등으로 대체됨
- `Petrichor.css` → `KimJaeeunPetrichorDesktop.css` 등으로 대체됨
- `Pledge.css`
- `SeeTinhAnimated.css` → `DoTienHongSeeTinhAnimatedMvDesktop.css` 등으로 대체됨
- `SeeTinhIsometric.css` → `DoTienHongSeeTinhIsometicDesktop.css` 등으로 대체됨
- `SimseongbinBoom.css` → `ShimSungbinBoomDesktop.css` 등으로 대체됨
- `SimseongbinLinked.css` → `ShimSungbinLinkedDesktop.css` 등으로 대체됨
- `SongdaheeCardOfLove.css` → `SongDaheeACardOfLoveDesktop.css` 등으로 대체됨
- `SongdaheePeony.css` → `SongDaheePeonyDesktop.css` 등으로 대체됨
- `WoosuminSasindo.css` → `WoosuminSasindoDesktop.css` 등으로 대체됨

## Media Query를 사용하는 통합 CSS 파일 (유지 필요)

다음 파일들은 Media Query를 사용하여 모든 디바이스를 하나의 파일로 처리합니다:
- `WoosuminLiminal.css` - Media Query로 Desktop/Tablet/Mobile 모두 처리

## 권장 사항

1. 위의 레거시 파일들은 사용되지 않으므로 삭제를 고려할 수 있습니다.
2. 하지만 삭제하기 전에 프로젝트 전체에서 이 파일들이 참조되는지 확인해야 합니다.
3. `WoosuminLiminal.css`는 Media Query를 사용하므로 유지해야 합니다.

